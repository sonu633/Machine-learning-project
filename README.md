# Machine-learning-project
There is most important project
# Requirements
. python 3.8
. pip( install all pakage of python)
.git

# clone Repository 
.git clone link of Repository
.cd project
# create virtual Environments 
python -m venv venv
venv\script\activate
# install Dependies
pip install -r requirements.txt
# Run the project
python main.py

